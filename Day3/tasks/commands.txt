# first made the WS
$ cd AucRobotics/Day3
$ mkdir -p AH-ws/src
$ cd AH-ws/src
$ catkin_init_workspace
$ cd ..
$ catkin_make
$ source devel/setup.bash

#then I created the pkg
$ mkdir src/pkgs
$ cd src/pkgs
$ catkin_create_pkg first-pkg std_msgs rospy roscpp sensor_msgs
$ cd ../..
$ catkin_make 

#finaly I edit the xml file
$ gedit src/pkgs/first-pkg/package.xml
// i made the edit and put my mail and name then save 
$ ^c

# after each task I made git add , git commit , git push
